# Fases 1–7 — Núcleo do Sistema

**Status:** ✅ Completo

---

## Fase 1 — RAG Pipeline
- PDF → ChromaDB via LangChain
- `IngestService`: chunk, embed, store
- Embeddings: `nomic-embed-text` via Ollama

## Fase 2 — Agente ReAct
- LangGraph `StateGraph` com loop ReAct
- Tool: `rag_search`
- LLM: Ollama `qwen2.5:7b`

## Fase 3 — Memória
- `summarize_node`: comprime histórico longo em `AgentState.summary`
- `trim_messages`: mantém janela de contexto controlada
- `SessionManager`: dict em memória por `session_id`

## Fase 4 — API FastAPI + Docker + Streamlit
- FastAPI com `StreamingResponse` SSE
- Docker Compose inicial
- Streamlit UI alternativa (`ui/` service)

## Fase 5 — BaseAgent (Template Method)
- `BaseAgent` ABC com `tools`, `system_prompt`, `build()`
- Separação entre estrutura do grafo e implementação específica

## Fase 6 — Skills + SKILL_REGISTRY + ConfigurableAgent
- `SKILL_REGISTRY`: dict de skills built-in
- `ConfigurableAgent`: monta tools a partir de `AgentConfig` (banco)
- Skills: `rag_search`, `web_search` (DuckDuckGo), `human_handoff`

## Fase 7 — Streamlit atualizado
- UI Streamlit atualizada para usar ConfigurableAgent
